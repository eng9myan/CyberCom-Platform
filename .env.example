# CyberCom Platform — Root Environment Variables
# Copy to .env and fill values. Never commit real secrets.

# ── Frontend ─────────────────────────────────────────────────────────────────
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_CYIDENTITY_ISSUER=http://localhost:8080/realms/cybercom
NEXT_PUBLIC_CYIDENTITY_CLIENT_ID=cybercom-frontend

# ── See backend/.env.example for backend variables ───────────────────────────
