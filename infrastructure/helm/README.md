# Helm Chart Library

> Implements parts of [`platform_engineering_baseline`](../../docs/platforms/platform_engineering_baseline.md).

Reusable charts (planned):
- `cybercom-service` — service baseline (probes, securityContext, HPA, PDB, NetworkPolicy, OTel, metrics)
- `cybercom-web` — Next.js app chart
- `cybercom-job` — Celery / async worker chart
